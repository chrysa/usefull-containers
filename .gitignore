.gitnexus

# Python
__pycache__/
*.pyc
*.pyo
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
*.egg-info/
.venv/
venv/

# Node / Frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/

# Docker
.docker/

# CodeGraph (local index — never commit)
.codegraph/

# graphify — local binary + generated artefacts (never commit)
/graphify
/sys
/graphify-out/

# Local dev override
docker-compose.override.yml

# E2E — Playwright
test-results/
tests/e2e/node_modules/
tests/e2e/playwright-report/
tests/e2e/test-results/
graphify
sys
graphify-out/

# chrysa canonical .gitignore — source of truth
# Source: chrysa/shared-standards/templates/gitignore.canonical
# Appended (not overwritten) into each repo's .gitignore by project-init and
# scripts/purge-artifacts.sh. Every entry here is a regenerable machine artifact,
# never source. Repo-specific ignores live ABOVE the managed block in each repo.
# ---8<--- chrysa:canonical:begin (managed — do not edit inside this block) --->8---

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.benchmarks/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/

# Node / frontend
node_modules/
.svelte-kit/
.turbo/
.vite/
*.tsbuildinfo

# Test / e2e output
test-results/
playwright-report/
.playwright-mcp/

# Local dev tooling caches (knowledge graph / code index)
graphify-out/
.codegraph/
.import_linter_cache/

# Claude Code local state
.claude/worktrees/
.claude/settings.local.json
.claude/*.local.md

# OS / editor
.DS_Store
Thumbs.db

# ---8<--- chrysa:canonical:end --->8---

# Generated audit report (guideline-checker) — never committed
guideline-report.html

# Local-only E2E fixtures (real .sav files — never committed)
tests/e2e/.local-fixtures/
# graphify artifacts (keep architecture diagram only)
graphify-out/*
!graphify-out/architecture.json
!graphify-out/architecture.html

# Session scratch (SDD ledger, run logs) — never committed
.superpowers/
