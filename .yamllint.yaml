extends: default
rules:
  colons:
    max-spaces-after: 1
    max-spaces-before: 0
  commas:
    max-spaces-after: 1
    max-spaces-before: 0
    min-spaces-after: 1
  indentation:
    indent-sequences: consistent
    level: warning
  line-length:
    max: 200
