---
pylint:dockerfile:
    extends: .analyse_dockerfile
    rules:
        - changes:
              - pylint/Dockerfile
    variables:
        service_name: pylint

pylint:build:
    extends: .build_service
    needs:
        - job: pylint:dockerfile
          optional: true
    rules:
        - changes:
              - pylint/**
    variables:
        DOCKER_REPO: ${CI_REGISTRY_IMAGE}
        service_name: pylint

pylint:version:
    extends: .publish_version_master
    needs:
        - pylint:build
    rules:
        - if: $CI_PIPELINE_SOURCE == "push" && $CI_BUILD_REF_NAME == $CI_DEFAULT_BRANCH
          changes:
              - pylint/docker-compose.yaml
              - pylint/Dockerfile
          variables:
              DOCKER_REPO: ${DOCKER_HUB_USER}
              service_name: pylint

pylint:latest:
    extends: .publish_latest
    needs:
        - pylint:version
    rules:
        - if: $CI_PIPELINE_SOURCE == "push" && $CI_BUILD_REF_NAME == $CI_DEFAULT_BRANCH
          changes:
              - pylint/**
    variables:
        service_name: pylint
pylint:release:
    extends: .release_job
    rules:
        - if: $CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH == "release/*"
          changes:
              - pylint/**
    variables:
        service_name: pylint
        version: $(cat .env | grep "PYLINT_CONTAINER_VERSION" | cut -d"=" -f1)
